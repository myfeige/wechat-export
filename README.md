# Claude Code Skills

我的 Claude Code 技能集合。

## 安装

将 `skills/` 目录复制到 `~/.claude/skills/`：

```bash
cp -r skills/* ~/.claude/skills/
```

## 已有技能

### wechat-export

导出微信好友聊天记录为 HTML（图片内嵌）+ TXT + JSON。

```
/wechat-export <好友备注>
```
